<?php

namespace App\Models\WakaKurikulum\Surat;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class SuratKelulusan extends Model
{
    use HasFactory;

    // protected $fillable = [
    //     // Tentukan field yang bisa diisi
    // ];

    // protected $table = 'surat_kelulusan';  // Misalkan model ini menggunakan nama tabel khusus
    protected $table = 'dummy_table';  // Misalkan model ini menggunakan nama tabel khusus

    // protected $primaryKey = 'id';  // Sesuaikan dengan primary key yang kamu gunakan

    // Tambahkan relasi atau metode lain jika diperlukan
}
